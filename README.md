# Movies.map

React frontend for Movies.map (Vite + React + JavaScript).

## Scripts

- `npm install` — install dependencies
- `npm run dev` — start local development server
- `npm run build` — production build
- `npm run preview` — preview production build
