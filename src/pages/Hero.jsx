import './Hero.css'

function Hero() {
  return (
    <section id="hero" className="snap-section hero" aria-label="Hero">
      <h1 className="section-title">Hero</h1>
    </section>
  )
}

export default Hero
